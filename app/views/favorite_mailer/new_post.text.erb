New post "<%= @post.title %>"
<%= "=" * (@post.title.length + 12) %>

By: <%= user.name %>

<%= @post.body %>

Visit online at: <%= topic_post_url(@post.topic, @post, anchor: "comment-#{@comment.id}") %>
You will be receiving updates for this post activity.
